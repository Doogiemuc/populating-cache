// @flow

/**
 * This function says hello.
 * @param name Some name to say hello for.
 * @returns The hello.
 */
const sayHello = function(name = "Haz") {
	return `Hello, ${name}!`;
}

export default sayHello;
